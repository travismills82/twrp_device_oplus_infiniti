diff --git a/KeyStorage.cpp b/KeyStorage.cpp
--- a/KeyStorage.cpp
+++ b/KeyStorage.cpp
@@ -337,6 +337,9 @@ static KeystoreOperation BeginKeystoreOp(Keystore& keystore, const std::string&
     auto blob_file = dir + "/" + kFn_keymaster_key_blob;
     LOG(INFO) << "reading blob_file: " << blob_file;
+    // Current TWRP vold no longer creates /tmp/keymaster_key_blob here.
+    // The legacy mkdir failure guard (errno != EEXIST) is therefore unnecessary;
+    // KeyMint-upgraded blobs remain in memory and the on-disk blob stays unchanged.
     // Never persist an upgraded blob: the installed system cannot open a blob
     // rebound to a newer OS version or patch level.
     // auto upgraded_blob_file = dir + "/" + kFn_keymaster_key_blob_upgraded;
